练习1：

User：表示类名
BaseModel：表示pydantic的一个数据基础类。
name: str：是类的一个属性，强制要求字符串类型
age: int：是类的一个属性，强制要求整数类型



练习2：

成功，int类型，因为pydantic会将合法的数值进行自动转换，比如将字符串类型的整数转为整数，但是其他字符串就不能转为整数，比如“abc”就会报错。



练习3：

因为原本定义的类的age属性要求是整数类型，所以一般要求该属性必须是整数类型，但是pydantic也会将字符串类型的整数自动转为整数，这不会报错，但是其它纯字符串的pydantic就无法自动转换为整数，所以会报错。



练习4：