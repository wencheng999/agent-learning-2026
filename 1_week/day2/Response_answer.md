练习1：

200请求响应成功。

401表示身份认证失败。

404表示通常是URL写错。

429表示表示请求太频繁或超过某种额度。

500表示服务器内部程序错误。



练习2；

response是服务器返回一个响应对象，里面包含具体的响应内容。

status_code是响应的状态码，代表成功或哪里出错。

text表示响应的字符串内容。



练习3：

data是dict格式，一般是

```
data["name"]
```

练习4：

response.raise_for_status()遇到错误会主动抛出，因此结合try/except能够有效处理错并知道错误是什么。