练习1：

return_exceptions=True 可以让某一个异常任务发生异常时仍能让整个流程不至于崩溃，可以将某一个异常任务发生的异常内容保存到结果中。



练习2：

A ValueError("失败") C



练习3：

部分失败就是某个流程需要执行这三个工具，然后执行这个三个工具时有两个成功，一个失败，不能让这一个失败影响其他成功的任务，从而不影响整个流程，然后根据失败的内容信息，让Agent根据失败内容检查失败任务的哪部分出现了错误，从而能够降级进行修补，从而尽可能让Agent全部自己执行完成整个流程任务。





